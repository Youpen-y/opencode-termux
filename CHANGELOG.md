# Changelog

## [Unreleased]

### Added
- Initial release: install/uninstall script for opencode on Termux
- Support for aarch64 architecture
- Run glibc binaries via glibc-runner (grun)
- Custom version via OPENCODE_VERSION environment variable
